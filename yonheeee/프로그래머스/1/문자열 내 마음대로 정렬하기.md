## 풀이방법
n의 알파벳을 기준으로 순서대로 정렬(만약에 같을 경우 문자열 앞부터 비교해서)

## 코드
```
import java.util.*;

class Solution {
    public String[] solution(String[] strings, int n) {
        String[] answer = new String[strings.length];
        String[] temp = new String[strings.length];
        for(int i = 0 ;i < strings.length; i++){
            StringBuilder sb = new StringBuilder();
            String s = strings[i];
            
            sb.append(s.charAt(n)).append(s);
            temp[i] = sb.toString();
        }
        
        Arrays.sort(temp);
        
        for(int i = 0; i < temp.length; i++){
            String s = temp[i];
            
            answer[i] = s.substring(1);
        }
        return answer;
    }
}
```

## 회고
처음에 n을 기준으로 같으면 당연하게 그 뒤를 기준으로 크기 비교라고 생각하고 풀었는데 실패가 뜸...</br>
알고보니 단어 앞부터 비교...</br>
그래..level1인데...</br>
**오늘은 교훈 : 문제를 제대로 읽자!!!!**

## AI 코드
```
import java.util.*;

class Solution {
    public String[] solution(String[] strings, int n) {
        
        Arrays.sort(strings, (a, b) -> {
            if (a.charAt(n) == b.charAt(n)) {
                return a.compareTo(b);
            }
            
            return a.charAt(n) - b.charAt(n);
        });
        
        return strings;
    }
}
```

### 처음에 잘못 푼 코드
```
// 같을경우, n기준 다음 알파벳 비교가 아닌 원래의 string기준으로 비교
// // n앞을 잘라서 뒤에 맨 뒤에 붙임 비교 후 정렬후 다시 원상태로 복구
// import java.util.*;
// class Solution {
//     public String[] solution(String[] strings, int n) {
        
//         String[] answer = new String[strings.length];
//         String[] temp = new String[strings.length];
        
//         //자르기
//         for(int i = 0; i < strings.length; i++){
//             StringBuilder sb = new StringBuilder();
//             String s = strings[i];
            
//             sb.append(s.substring(n)).append(s.substring(0,n));
//             //System.out.println(sb);
//             temp[i] = sb.toString(); 
//         }
        
//         //정렬 
//         Arrays.sort(temp);
//         //System.out.println(Arrays.toString(temp));
        
//         //붙이기
//         for(int i = 0; i < temp.length; i++){
//             StringBuilder sb = new StringBuilder();
//             String s = temp[i];
//             //System.out.println(Arrays.toString(temp));
                
//             sb.append(s.substring(s.length()-n)).append(s.substring(0,s.length()-n));
//             answer[i] = sb.toString();      
//         }
        
        
        
//         return answer;
//     }
// }
```