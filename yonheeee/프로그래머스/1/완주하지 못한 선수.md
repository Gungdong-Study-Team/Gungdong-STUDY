## 문제풀이
completion에 없는 participant를 구하는 문제</br>
HashMap을 이용해 <이름, 인원수>를 넣고 비교하여 동명이인일 경우 하나만 나오게</br>

## 코드
```
// HashMap으로 이름과 인원수를 같이 저장
// get, put -> O(N)
import java.util.*;

class Solution {
    public String solution(String[] participant, String[] completion) {
        String answer = "";
        
        HashMap<String, Integer> map = new HashMap<>();
        
        for(String str : participant){
            //getOrDefault는 key의 숫자 0은 기본
            map.put(str, map.getOrDefault(str, 0)+1);
        }
        
        for(String str : completion){
            if(map.containsKey(str)){
                map.put(str,map.get(str)-1);
            }
        }
        // keySet은 Map 안의 모든 key만 모아서 Set형태로 가져옴
        for(String key : map.keySet()){
            if(map.get(key) == 1){
                answer = key;
            }
        }
 
        return answer;
    }
}

// 시도2 : 효율성 테스트에서 통과X 
// List에서 contains()와 remove()가 O(N) -> O(N^2)
// class Solution {
//     public String solution(String[] participant, String[] completion) {
        
//         List<String> list = new ArrayList<>();
        
//         for(String str : participant){
//             list.add(str);
//         }
        
//         for(String str : completion){
//             if(list.contains(str)){
//                 list.remove(str);
//             }
//         }
//         //System.out.println(list);
//         return list.get(0);
//     }
// }

// 시도1 : 처음에 당연히 중복이라고 해서 set으로 없앨 생각했는데 set은 같은 값이 있으면 합쳐버림 -> 동명이인 구할 수 없음
// class Solution {
//     public String solution(String[] participant, String[] completion) {
//         String answer = "";
        
//         Set<String> set = new HashSet<>();
        
//         for(int i = 0; i < participant.length; i++){
//             set.add(participant[i]);
//             //System.out.print(participant[i]);
//         }
        
//         for(String str : completion){
//             if(set.contains(str)){
//                 set.remove(str);
//             }
//         }
//         System.out.print(set);
//         return answer;
//     }
// }
```

## 회고
정식적으로 HashMap에 대해서 공부하게 된 계기...</br>
처음에는 중복이어서 당연히 Set으로 풀었는데 동명이인을 무시함(문제 잘 읽자..)</br>
아이디어가 생각나지 않아 List로 풀었는데 효율성 테스트에서 모두 실패...</br>
아이디어를 지피티 행님에게 여쭤봤더니 HashMap으로 <이름, 인원수> 저장..하라던</br>
덕분에 정식으로 공부함...</br>
알고리즘 kit 해시 다 풀어봐야지

## AI 코드
```
import java.util.*;

class Solution {
    public String solution(String[] participant, String[] completion) {
        Map<String, Integer> map = new HashMap<>();

        // 참가자 인원수 저장
        for (String name : participant) {
            map.put(name, map.getOrDefault(name, 0) + 1);
        }

        // 완주한 사람 한 명씩 제거
        for (String name : completion) {
            map.put(name, map.get(name) - 1);
        }

        // 1명이 남은 사람이 미완주자
        for (String name : map.keySet()) {
            if (map.get(name) > 0) {
                return name;
            }
        }

        return "";
    }
}
```
