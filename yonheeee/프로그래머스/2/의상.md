### 풀이방법
조합 -> 조합으로 숫자 뽑아서 숫자에 맞는 배열 중복 없으면 증가</br>
해시 -> ~ ing

### 코드
**조합 코드(시간 초ㄱ허)**
```
import java.util.*;
class Solution {
    static int[] sel;
    static String[][] clothes;
    static int answer;
    
    public int solution(String[][] clothes) {
        //Map<String, String> map = new HashMap<>();
        answer = clothes.length;
        this.clothes = clothes;
        
        // for(int i = 0; i < clothes.length; i++){
        //     map.put(clothes[i][0],clothes[i][1]);
        // }
        
        
        for(int size = 2; size <= clothes.length; size++){
            sel = new int[size];
            comb(0,0, size, clothes.length);
        }
        
        return answer;
    }
    
    private void comb(int start, int idx, int size,int clothe){
        if(idx == size){
           //System.out.println(Arrays.toString(sel));
            List<String> list = new ArrayList<>();
            
             for(int i = 0; i < size; i++){
                String type = clothes[sel[i]][1];
    
                if(list.contains(type)){
                    return;
                }  
                list.add(type);
            }
            answer++;
            return;
        }
        
        for(int i = start; i < c lothe; i++){
            sel[idx] = i;
            comb(i+1, idx+1, size,clothe);
        }
    }
}
```
**해시 코드**
```
//~ing
```

### 회고
해시라는 것을 알지만, 그걸 구현해내지 못하는 나의 실력</br>
조합 뽑는것도 한세월...</br>
해시 공부하고 다시 풀어볼게요</br>

### AI가 수정해준 코드
```
(해시 진행 후 물어볼 예정)
```