### 풀이방법
COUNT를 사용하여 수 측정

### 코드
```
SELECT COUNT(DISTINCT NAME)
FROM ANIMAL_INS;
```

### 회고
DISTINCT는 알고 있었는데 COUNT안에 쓰는건 또 처음알았네..</br>


### 공부 내용
COUNT(*) : null, 중복 모두 포함</br>
COUNT(칼럼) : null이 아닌 모든 것(null은 세지 않음)</br>
COUNT(DISTINCT 칼럼) : null, 중복 제외