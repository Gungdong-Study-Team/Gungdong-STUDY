SELECT P.PRODUCT_CODE, P.PRICE * IFNULL(SUM(O.SALES_AMOUNT),0) AS SALES
FROM PRODUCT AS P
INNER JOIN OFFLINE_SALE AS O ON (P.PRODUCT_ID = O.PRODUCT_ID)
GROUP BY P.PRODUCT_CODE
ORDER BY SALES DESC, P.PRODUCT_CODE ASC;